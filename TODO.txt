1. make commit-msg more robust.

2. make pre-commit more robust.

[*] 3. update and revise docker entrypoints.

4. consider to use .sh files for ci for your commands.

[*] 5. use npm ci --ignore-scripts

6. replace readme file.

7. consider to use npx instead of npm if your packages do not need to be install inside node_modules.
  also consider to use --no-install if packages need to be installed locally.

8. understand the docker-compose files and update if any changes needed.

9. in your github workflows first use the docker images for your steps if it does exist.
  if it's not use your npm packages with the related packages.
  then use your .sh custom commands if no one of them exists.

10. do more usable your actions. understand what is your flow of the workflow.
  add more thing if you need.

11. DDD file structure.

12. consider pre-push commands if you need.

13. CQRS design.

14. setup Healtycheck api

15. production docker image.

16. consider to use set command inside bash for better error handling.

17. use a docker image for your custom scripts for a better handling inside cicd and or somewhere else.
 
18. create an auto pipeline to watch a folder for your custom scripts to build a new image and push it to dockerhub.
  notice to consider security checks for the image.
